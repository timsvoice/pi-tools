# yaml-language-server: $schema=https://promptfoo.dev/config-schema.json

description: Scribe decision intake prompt evaluation

prompts:
  - file://extensions/scribe/prompts/scribe.md

providers:
  - id: openrouter:openai/gpt-5-mini
    config:
      temperature: 0
      showThinking: false

defaultTest:
  options:
    provider: openrouter:openai/gpt-5-mini

tests:
  - file://tests/promptfoo/scribe.tests.yaml
