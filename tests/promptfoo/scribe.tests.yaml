- description: captures explicit project policy
  vars:
    recentTurns: |-
      User: Can we loosen the log size limits for testing?
      Assistant: We can revisit later.
      User: I just need a quick experiment.
      Assistant: Let's avoid changing defaults without a reason.
      User: Project policy: All changes must run npm test before commit. Why: avoids shipping untested changes. Impact: no commits without green tests. Invalidate if CI enforces pre-merge tests.
      Assistant: Noted.
      User: Also, keep responses short.
      Assistant: Acknowledged.
  assert:
    - type: javascript
      value: |
        let parsed;
        try {
          parsed = JSON.parse(output);
        } catch {
          return false;
        }
        return parsed.status === "decision"
          && typeof parsed.title === "string"
          && parsed.title.trim().length > 0
          && typeof parsed.type === "string"
          && parsed.type.length > 0
          && typeof parsed.decision === "string"
          && parsed.decision.trim().length > 0
          && typeof parsed.why === "string"
          && parsed.why.trim().length > 0
          && typeof parsed.impact === "string"
          && parsed.impact.trim().length > 0
          && typeof parsed.invalidation === "string"
          && parsed.invalidation.trim().length > 0;
    - type: llm-rubric
      value: |-
        PASS if the decision field faithfully captures the rule "all changes must run npm test before commit" and the why, impact, and invalidation fields contain meaningful content derived from the conversation (not filler or restatements of the decision).
        FAIL if the decision field distorts the rule, or if the why/impact/invalidation fields are generic filler unrelated to the conversation.

        PASS example:
        {"status":"decision","title":"Run npm test before commit","type":"CONSTRAINT","decision":"All changes must run npm test before commit.","why":"Avoids shipping untested changes.","impact":"No commits without green tests.","invalidation":"CI enforces pre-merge tests."}

        FAIL example:
        {"status":"decision","title":"Testing policy","type":"CONSTRAINT","decision":"Run npm test and commit frequently.","why":"Testing is important.","impact":"Better code quality.","invalidation":"When no longer needed."}

- description: ignores local refactor
  vars:
    recentTurns: |-
      User: The file feels cluttered.
      Assistant: We can do a cleanup pass.
      User: I renamed the helper and moved it into utils for clarity.
      Assistant: Done.
      User: Should we capture that as a convention?
      Assistant: No, it's a local refactor.
  assert:
    - type: javascript
      value: |
        let parsed;
        try {
          parsed = JSON.parse(output);
        } catch {
          return false;
        }
        return parsed.status === "no_decision"
          && parsed.title === ""
          && parsed.type === ""
          && parsed.decision === ""
          && parsed.why === ""
          && parsed.impact === ""
          && parsed.invalidation === "";

- description: captures durable API boundary decision
  vars:
    recentTurns: |-
      User: Can we add extra logging while debugging reloads?
      Assistant: Temporary logs are fine.
      User: The last run corrupted DECISIONS.md when two writes collided.
      Assistant: That sounds like a race.
      User: Decision: The API boundary owns input validation; internal services should never validate raw request payloads. Why: shared validation rules drifted and caused inconsistent behavior. Impact: all new endpoints must validate at the API boundary before calling service logic. Invalidate if we introduce a dedicated validation service used by all layers.
      Assistant: Noted.
      User: Let's rename the handler.
      Assistant: Renames are fine.
  assert:
    - type: javascript
      value: |
        let parsed;
        try {
          parsed = JSON.parse(output);
        } catch {
          return false;
        }
        return parsed.status === "decision"
          && typeof parsed.decision === "string"
          && parsed.decision.trim().length > 0
          && parsed.why.trim().length > 0
          && parsed.impact.trim().length > 0
          && parsed.invalidation.trim().length > 0
          && parsed.decision.length < 220;
    - type: llm-rubric
      value: |-
        PASS if the decision field states that the API boundary owns input validation and internal services must not validate raw request payloads. The meaning must match the conversation — not just contain the right keywords.
        FAIL if the decision field contains the keywords "API boundary" and "validation" but distorts the rule (e.g., says validation is optional, or assigns ownership to internal services).

        PASS example:
        {"status":"decision","title":"API boundary owns input validation","type":"INTERFACE","decision":"Validate raw request payloads at the API boundary; internal services must not validate raw request payloads.","why":"Shared validation rules drifted and caused inconsistent behavior.","impact":"All new endpoints must validate at the API boundary before calling service logic.","invalidation":"Introduce a dedicated validation service used by all layers."}

        FAIL example:
        {"status":"decision","title":"API validation","type":"INTERFACE","decision":"The API boundary has validation for request payloads.","why":"Validation is needed.","impact":"Endpoints should validate.","invalidation":"Not specified."}

- description: collapses repeated rationale into one rule
  vars:
    recentTurns: |-
      User: We should avoid mixing persistence writes into business logic.
      Assistant: Agreed.
      User: It's caused validation to drift in the past.
      Assistant: Yes.
      User: Decision: All persistence writes must go through the data layer. Why: direct writes caused inconsistent validation. Impact: new features must route writes through data layer APIs. Invalidate if the data layer is replaced.
      Assistant: Noted.
  assert:
    - type: javascript
      value: |
        let parsed;
        try {
          parsed = JSON.parse(output);
        } catch {
          return false;
        }
        return parsed.status === "decision"
          && typeof parsed.decision === "string"
          && parsed.decision.trim().length > 0
          && parsed.why.trim().length > 0
          && parsed.impact.trim().length > 0
          && parsed.invalidation.trim().length > 0
          && parsed.decision.length < 220;
    - type: llm-rubric
      value: |-
        PASS if the decision field is a concise rule about data-layer write ownership (all persistence writes go through the data layer), stated as a durable invariant — not a narrative retelling of the conversation.
        FAIL if the decision retells the conversation ("we discussed...", "after noticing drift...") or is vague enough to be useless as a rule.

        PASS example:
        {"status":"decision","title":"Data layer owns persistence writes","type":"ARCHITECTURAL","decision":"All persistence writes must go through the data layer.","why":"Direct writes caused inconsistent validation.","impact":"New features must route writes through data layer APIs.","invalidation":"The data layer is replaced."}

        FAIL example:
        {"status":"decision","title":"Write discussion","type":"ARCHITECTURAL","decision":"We discussed avoiding mixing persistence writes into business logic and decided the data layer should handle it.","why":"It caused problems before.","impact":"Things should go through the data layer.","invalidation":"If we change our minds."}

- description: ignores UX copy tweak
  vars:
    recentTurns: |-
      User: The footer status is noisy.
      Assistant: We can tweak the display copy.
      User: Let's change the status text from Scribing... to Writing... for clarity.
      Assistant: Okay.
      User: Also, align timestamps to UTC.
      Assistant: That's a UI preference.
  assert:
    - type: javascript
      value: |
        let parsed;
        try {
          parsed = JSON.parse(output);
        } catch {
          return false;
        }
        return parsed.status === "no_decision"
          && parsed.title === ""
          && parsed.type === ""
          && parsed.decision === ""
          && parsed.why === ""
          && parsed.impact === ""
          && parsed.invalidation === "";

- description: ignores session-only choice
  vars:
    recentTurns: |-
      User: We discussed polling vs webhooks and used webhooks in this task.
      Assistant: Sounds good.
  assert:
    - type: javascript
      value: |
        let parsed;
        try {
          parsed = JSON.parse(output);
        } catch {
          return false;
        }
        return parsed.status === "no_decision"
          && parsed.title === ""
          && parsed.type === ""
          && parsed.decision === ""
          && parsed.why === ""
          && parsed.impact === ""
          && parsed.invalidation === "";

- description: ignores temporary debug logging
  vars:
    recentTurns: |-
      User: Added a console.log to debug the handler for now.
      Assistant: Will remove later.
  assert:
    - type: javascript
      value: |
        let parsed;
        try {
          parsed = JSON.parse(output);
        } catch {
          return false;
        }
        return parsed.status === "no_decision"
          && parsed.title === ""
          && parsed.type === ""
          && parsed.decision === ""
          && parsed.why === ""
          && parsed.impact === ""
          && parsed.invalidation === "";

- description: ignores migration mechanics
  vars:
    recentTurns: |-
      User: Keep the legacy fallback while we migrate configs.
      Assistant: Okay for now.
  assert:
    - type: javascript
      value: |
        let parsed;
        try {
          parsed = JSON.parse(output);
        } catch {
          return false;
        }
        return parsed.status === "no_decision"
          && parsed.title === ""
          && parsed.type === ""
          && parsed.decision === ""
          && parsed.why === ""
          && parsed.impact === ""
          && parsed.invalidation === "";

- description: ignores placeholder chatter
  vars:
    recentTurns: |-
      User: The template should keep {Short title} for now.
      Assistant: That's just an example placeholder.
  assert:
    - type: javascript
      value: |
        let parsed;
        try {
          parsed = JSON.parse(output);
        } catch {
          return false;
        }
        return parsed.status === "no_decision"
          && parsed.title === ""
          && parsed.type === ""
          && parsed.decision === ""
          && parsed.why === ""
          && parsed.impact === ""
          && parsed.invalidation === "";

- description: enforces json-only output
  vars:
    recentTurns: |-
      User: Project policy: All changes must run npm test before commit. Why: avoid shipping untested changes. Impact: no commits without green tests. Invalidate if CI enforces pre-merge tests.
  assert:
    - type: javascript
      value: |
        const trimmed = output.trim();
        if (!trimmed.startsWith("{") || !trimmed.endsWith("}")) {
          return false;
        }
        try {
          JSON.parse(trimmed);
        } catch {
          return false;
        }
        return !trimmed.includes("```")
          && !trimmed.startsWith("Note:")
          && !trimmed.startsWith("Summary:")
          && !trimmed.startsWith("Thanks")
          && !trimmed.startsWith("Thank you")
          && !trimmed.startsWith("Sure");

- description: ignores quoted prose and still returns json
  vars:
    recentTurns: |-
      User: > You know the feeling. You sit down with your agent, start pulling a thread, and the hours melt away.
      Assistant: Let's capture decisions only.
      User: Decision: Use Node.js 22+ for tooling. Why: aligns with platform support. Impact: update local runtimes. Invalidate if platform drops Node 22.
  assert:
    - type: javascript
      value: |
        const trimmed = output.trim();
        if (!trimmed.startsWith("{") || !trimmed.endsWith("}")) {
          return false;
        }
        try {
          JSON.parse(trimmed);
        } catch {
          return false;
        }
        return !trimmed.startsWith(">")
          && !trimmed.includes("\n>")
          && !trimmed.includes("```");

- description: preserves scoping constraint under compression
  vars:
    recentTurns: |-
      User: We keep getting alert fatigue from staging errors.
      Assistant: Staging noise is a known problem.
      User: What about just reducing the alert volume?
      Assistant: That risks missing real issues.
      User: Decision: Enable error alerting only in production, not staging. Why: staging generates false positives that desensitize the team to real production errors. Impact: alerting config must check the environment before firing. Invalidate if staging stability reaches production-grade reliability.
      Assistant: Noted.
  assert:
    - type: javascript
      value: |
        let parsed;
        try {
          parsed = JSON.parse(output);
        } catch {
          return false;
        }
        return parsed.status === "decision"
          && typeof parsed.decision === "string"
          && parsed.decision.trim().length > 0
          && parsed.decision.length < 220
          && parsed.why.trim().length > 0
          && parsed.impact.trim().length > 0
          && parsed.invalidation.trim().length > 0;
    - type: llm-rubric
      value: |-
        PASS if the decision field preserves both sides of the scoping constraint: alerting is enabled in production AND disabled in staging. Both parts must be present — dropping "not staging" distorts the rule.
        FAIL if the decision mentions production alerting but omits the staging exclusion, or generalizes to "enable alerting" without the environment constraint.

        PASS example:
        {"status":"decision","title":"Production-only error alerting","type":"CONSTRAINT","decision":"Enable error alerting only in production; do not alert on staging errors.","why":"Staging false positives desensitize the team to real production errors.","impact":"Alerting config must check the environment before firing.","invalidation":"Staging stability reaches production-grade reliability."}

        FAIL example:
        {"status":"decision","title":"Error alerting","type":"CONSTRAINT","decision":"Enable error alerting to catch production issues.","why":"Errors need to be caught.","impact":"Set up alerting.","invalidation":"When not needed."}
