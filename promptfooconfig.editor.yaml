# yaml-language-server: $schema=https://promptfoo.dev/config-schema.json

description: Editor conventions prompt evaluation

prompts:
  - file://extensions/scribe/prompts/editor.md

providers:
  - id: openrouter:openai/gpt-5-mini
    config:
      temperature: 0
      showThinking: false

defaultTest:
  options:
    provider: openrouter:openai/gpt-5-mini

tests:
  - file://tests/promptfoo/editor.tests.yaml
